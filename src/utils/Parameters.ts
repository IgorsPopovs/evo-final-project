export const stageParams = {
    height : 900,
    width: 1600,
    scale: 1,
};

export const cardParams = {
    height: stageParams.height / 6,
    width: stageParams.height / 9,
}

export const chipParams = {
    diameter: stageParams.height / 9,
}